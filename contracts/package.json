{
  "name": "drip-contracts",
  "version": "0.1.0",
  "description": "Drip — Agentic streaming protocol on Somnia. Smart contract workspace.",
  "license": "MIT",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network somniaTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network somniaMainnet",
    "smoke-test": "hardhat run scripts/test-agent-invocation.ts --network somniaTestnet",
    "test:classifier": "hardhat run scripts/test-classifier.ts --network somniaTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@types/node": "^20.10.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.3.0",
    "viem": "^2.7.0",
    "dotenv": "^16.4.0"
  },
  "dependencies": {
    "@somnia-chain/reactivity-contracts": "^0.1.0",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
